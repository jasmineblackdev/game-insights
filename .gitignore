# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
test-results
playwright-report
dist
dist-ssr
*.local
# Exclude every .env variant. The only checked-in file is .env.example
# (documents required vars without values). Production secrets live in
# the deployment platform's env settings + Supabase Edge function
# secrets — never in this repo.
.env
.env.*
!.env.example

# Supabase CLI local cache
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
